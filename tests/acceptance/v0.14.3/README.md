# v0.14.3 人工验收截图清单

未执行真实 GUI 验收前，不创建或伪造截图。

使用 CI Artifact 逐项检查：

- 100%、125%、150% DPI 下标题、副标题、服务状态、端口和按钮文字完整显示；
- 默认与最小窗口下，PPT 规则列表至少可显示三条或以滚动方式完整访问；
- 规则标题、路径、状态标签和启用按钮不重叠、不裁切；
- 色块、输入框和操作区不出现异常换行或原生立体页签。

截图必须遮挡二维码、token、完整 URL、局域网 IP、私人文件名和路径。
