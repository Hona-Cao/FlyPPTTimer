# FlyPPTTimer v0.18.5

本版本继续完善计时窗口窄尺寸显示和文件规则管理。

- 规则列表删除重复状态，只保留启用/禁用操作，并修复点击后滚动到顶部的问题。
- 支持勾选多条文件规则，批量修改计时时长和正计时/倒计时方式。
- 取消固定的计时窗口最小宽度，自动扩宽只由当前时间文字的实际安全宽度触发。
- 计时字体固定为微软雅黑；移除单页计时预留项。
- “黑屏并显示时间到”会结束放映、停止并重置计时器；“仅提示”可继续显示超时。
- 打开的 PowerPoint 文稿默认最大化。
