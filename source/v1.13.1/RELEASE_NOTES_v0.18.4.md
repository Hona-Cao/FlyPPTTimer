# FlyPPTTimer v0.18.4

本版本修复 v0.18.3 中仍存在的悬浮窗单边向右移动问题。根因是窗口在按 96 DPI 逻辑尺寸定位后，第一次显示时又被 WinForms 自动缩放，新增宽度全部落在右侧。现在程序会先读取目标显示器 DPI、计算最终物理尺寸，再围绕精确中心定位，并禁止显示阶段重复缩放。

同时修复设置窗口和主程序在第一次应用后共享配置对象的问题。修改默认位置或水平、垂直偏移时，程序现在能够正确比较应用前后的值，不会误判为位置未变化。
