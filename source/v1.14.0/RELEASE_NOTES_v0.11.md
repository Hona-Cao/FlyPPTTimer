# FlyPPTTimer v0.11

本版本在原有手机计时遥控中加入 Microsoft PowerPoint 控制，继续共用同一端口、token、二维码和网页。

主要新增：

- 计时/演示双页签手机界面；
- PowerPoint 放映、翻页、跳页、黑屏、白屏和结束控制；
- 当前已打开文稿与启用文件规则的安全选择；
- 独立 STA COM 控制服务、中文错误反馈和日志；
- PowerPoint 放映与计时器规则匹配、启动和结束重置联动。

本版本只承诺 Microsoft PowerPoint 远程控制，不将 WPS 标记为已支持。
