# v0.15.0 审计记录

## 范围

本版仅重设计 PC 远程控制窗口。替换文件为 `RemoteControlForm.cs`，新增专用 `RemoteDashboardTheme.cs`，并更新远控 UI 契约测试。共享 `ModernTheme.cs` 和所有业务服务、模型、手机网页均不在本版改动范围内。

## 架构

- 固定侧边导航承载页面切换。
- 连接页使用状态、二维码和浏览器访问卡片。
- 演示文稿页使用主从双栏工作区，规则列表和详情操作并列。
- UI 继续调用现有公开服务接口，不新增或修改远控协议命令。

## 自动化与人工验收

本地 Release 构建 0 警告、0 错误，自动化测试 42/42 通过，启动持续运行冒烟通过。真实构建、测试和本地发布结果记录在 `TEST_REPORT.md`。100%、125%、150%、200% DPI，Office/WPS、双屏、演讲者视图和手机联动仍待人工验收，不以源码检查代替真实验收。
