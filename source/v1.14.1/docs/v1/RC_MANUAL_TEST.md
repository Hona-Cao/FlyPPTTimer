# 1.13.0 第二轮反馈：剩余设备与体验确认

更新：2026-09-12。分支：`codex/v1-06-manual-test`。只使用本轮最终 `FlyPPTTimer-v1.13.0-home-feedback-20260912-win-x64.zip`，源码 SHA 见 BUILD.txt。解压到新目录，保留两个 DLL；不覆盖旧程序或配置。空 token 首次运行自动生成。

用户已经确认的最终尺寸稳定、滚动条、作者说明、手机基本控制、音频基本播放和全屏黑屏继续视为通过，不要求重做。程序侧完成情况与验证范围见 CODEX_RESULT.md。

1. **混合 DPI 拖动残余（仍未解决）**：具备 150%/125% 双屏时，只确认 Settings、PC Remote 鼠标仍按住跨边界时的瞬时内容错位。新增 Timer 字号/页码预览在跨 DPI 后也确认一次。家用机两屏都是 125%，不能替代此项。
2. **真实手机恢复与触控**：已有连接下操作约 10 分钟，锁屏 20–30 秒、切后台、关闭再恢复 Wi-Fi；token/port 不变时无需重新扫码，Next/Close 不应执行两次。顺带确认隐藏入口、恢复、上下移动、删除规则与重新添加已打开文稿的触控体验；删除只删列表，不删文件。桌面 HTTP、配置持久化和普通浏览器已验证。
3. **独立原生 Microsoft PowerPoint**：仅用可丢弃三页 A/B，A 放映后直接 Open B、从头启动 B、从编辑第 2 页启动 B；旧放映应结束，B 规则接管，A 仍打开且 dirty 状态不变。再确认关闭正在放映的目标会先结束放映。WPS 已完成 Open/两种启动切换，当前页实测为第 1 页；末尾追加关闭测试因 COM worker 超时未取得通过证据。本机 PowerPoint COM 被 WPS 接管，原生 PowerPoint 不标通过。
4. **新增边界体验**：具备 150% 时留意长文本导入/恢复默认/错误确认是否裁字或遮按钮（125% 未应用修改、批量错误和 Web 确认已测）。若使用超过 10 秒提示音，只确认本轮新增的 10 秒可听上限；无需重测原来的音频基本功能。不要为测试实际执行安装更新。

反馈项目编号和现象即可；截图遮住二维码、token 和私人路径。本包等待审核，不是 Release/Tag。
