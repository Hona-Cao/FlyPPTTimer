# FlyPPTTimer v0.30.2 本地测试版

本版本尚未发布到 GitHub 或 Gitee，仅供本机实测。

## 修复内容

- 缩小设置下拉框圆角；收起状态改为完全自绘，清除原生控件残留的上边线和左边线。
- 禁用单元格使用无边框实色圆角绘制，不再叠加灰色覆盖层，也不再出现半透明边缘。
- 恢复远程控制导航按钮完整的下半部和四个圆角，横向分隔线保持 1px。
- 演示文稿列表仅允许纵向滚动；长文件名使用省略号，不再生成横向滚动条。
- 设置窗口在实时缩放期间不再逐像素重建窗口圆角区域。
- 远程控制窗口在实时缩放期间暂停整页响应式重排与演示文稿轮询，结束缩放后再统一同步。

## 验证

- Release 构建：0 警告、0 错误。
- 自动化测试：188 项全部通过。
- 本机界面检查：设置下拉框/禁用色块、远程导航及演示文稿页面已检查。
