# FlyPPTTimer v0.18.8

本版修复 v0.18.6 实机反馈的三个问题。

- 100×30 等紧凑尺寸下，时间按实际可见字形边界水平、垂直居中，不再贴底或向右偏移。
- 手机端计时页与演示页始终显示“时间到”黑屏控制按钮；没有黑屏时会明确显示当前不可操作。
- WPS 打开演示文稿时直接捕获最外层 `PP12FrameClass` 窗口并在首次显示时最大化，打开完成后还会再次检查。
