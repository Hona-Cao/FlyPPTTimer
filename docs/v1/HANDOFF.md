# FlyPPTTimer V1 — 当前交接

## 最新状态：RC3.2 绿色包已构建，等待本轮新增功能验收

日期：2026-09-12。程序版本 1.13.0；Rust 1.92.0；不升级依赖，不创建 Release/Tag，不合并 main。

**Codex 继续暂停。当前没有新的实施任务，不执行旧 RC-3.1 阻断清单。**

## 必须区分的源码与交接分支

- 用户此前测试的 RC3.1 小修源码：`5bc223bd7bea12339311bac4494f369bb3d363b1`。
- 当前 RC3.2 测试包唯一产品源码：`fadf20323d5ed3ce42845bcf16017deb4eae2207`。
- 当前已提交源码分支：`chatgpt/rc32-verified-34687385876`。
- 构建辅助分支：`chatgpt/rc32-feedback`；它不是最终产品源码基点。
- 本 `codex/v1-06-manual-test` 分支目前只同步当前交接/结果文档，尚未合入上述独立测试源码。不能从本分支旧源码重新构建后冒充 RC3.2 包。

## 当前入口

1. `docs/v1/CODEX_TASK.md`：暂停及等待边界。
2. `docs/v1/RC32_DELIVERY.md`：已核实的实现、构建/测试结果、EXE/ZIP 哈希、源码和 artifact 定位。
3. 最终源码中的 `docs/v1/USER_FEEDBACK_20260912_3.md`、`docs/v1/APPROVED_PRODUCT_DEVIATIONS.md`：本轮批准的行为。
4. 最终源码中的 `docs/v1/RC32_MANUAL_TEST.zh-CN.md`；测试包中同名内容为 `MANUAL_TEST.zh-CN.md`。

旧 `CODEX_RESULT.md` 中的 a644e74/72 项等内容属于历史记录，不代表本轮最终源码或测试结论。最终 Windows run 34687385876 已完成：fmt、Clippy 严格检查、80 passed / 0 failed / 3 ignored、JS syntax、Release build、软件渲染布局截图。3 个 ignored 仍为需要真实音频/Office 条件的项目，不标为通过。下载包已经校验 CRC、PE x64、EXE SHA256 和 BUILD.txt 一致。

## 本轮已实现

- 自动/自定义窗口尺寸；自动隐藏宽高设置，自定义保留尺寸；即时预览、取消/应用。
- 页数与时间等上下留白；独立字号/颜色（默认跟随时间）、斜体、左右/居中对齐、上方/下方位置。
- 大屏所在显示器排除小计时器及镜像，停用后恢复。
- 手机按名称/文件大小/修改时间排序及升降序、持久化手动顺序。
- “移除文件”真正撤销受控列表资格，不删除磁盘文件、不关闭或保存文稿；显式重新添加后恢复。
- 当前放映高亮、上下移动画、长按拖动/邻居让位/边缘滚动、取消手势保护、禁选字复制，数字字段仍可输入。

## 用户手测边界

仅四组：尺寸/页数样式、大屏去重、手机排序/拖动、移除/重新添加/放映高亮。具体步骤及通过标准以 RC32_DELIVERY 和包内中文说明为准。真实手机手势、Office 和多显示器仍需用户本轮确认，软件渲染或模拟浏览器截图不是实机验收。

用户明确表示其余没有提到的问题暂时通过。不要再重新派发历史全量测试或无证据的全仓库整改；不要宣称这等于本轮重新测过所有旧功能。此前混合 DPI 等记录保留为历史背景，除用户反馈回归外不擅自扩大本轮任务。

## 后续协作

用户将测试结果反馈给 ChatGPT；ChatGPT 对失败项定向修正或明确安排 Codex。只有当前 CODEX_TASK 被再次改成实施任务后，Codex 才继续。两台电脑环境均已配置，不重新安装/升级工具链；以 GitHub 明确的最新产品源码为准，保护未提交本地修改，不强推、不关闭杀软/防火墙/鉴权，不操作用户真实工作文稿。
