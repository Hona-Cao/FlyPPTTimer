# FlyPPTTimer V1 — 新会话交接

## 产品目标

V1 是对 v0.30.2 的 Rust + Slint 重构。用户要求全部既有功能稳定运行，界面美观、配合舒服、风格统一和谐；当前整体排版已基本满意，不做无目的的大规模 UI 重做，不因重构削减功能。用户只承担最终体验确认，不承担反复 Debug。

## 当前分支与阅读顺序

Review 分支：`codex/v1-06-manual-test`。先 fetch/pull 最新 HEAD，保留用户未提交修改，不依赖旧聊天提交号。

以根目录 `AGENTS.md` 为准。特别注意 `APPROVED_PRODUCT_DEVIATIONS.md` 优先于冲突的旧基线：

- Remote 不暴露随机端口控件，固定端口优先，不可用时替换并保存；
- PC Remote 的“演示文稿”页只做规则管理，不恢复演示状态/放映控制按钮；手机/浏览器 Web Remote 保留完整演示控制。

## 最新状态：终审完成，代码冻结，等待用户统一手测

日期：2026-09-11。

Codex RC-2 验证提交：`633a830d78ca02be05994364d86f93eaf40a263e`。
候选源码基点：`952226d4c114b59334b13d0a7b44c60d89b9f030`。
版本：`1.13.0`；项目 Rust：`1.92.0`。

RC-2 和本次终审均只改文档，没有新的产品源码修改。ChatGPT 已核对提交差异和执行记录，接受为冻结候选版，但未批准“所有功能/实际观感均已验证”的正式发布。

**本轮没有新的编码任务。** 最新唯一指令仍是 `CODEX_TASK.md`，现已改为停止主动扫描/改代码，等待用户反馈。

用户所有剩余手测统一在：

`docs/v1/RC_MANUAL_TEST.md`

该文件包含终审结论、产物位置、无需重复的已测范围、五组操作/通过标准和反馈模板。不要再把各历史版本的待手测条目分别派给用户。

产物/候选记录：`docs/v1/RC_CANDIDATE.md`。
完整执行证据及限制：`docs/v1/CODEX_RESULT.md`。

## 已通过此前源码审核的主要收口

- F3 暂停后恢复，不清零进度；
- Settings baseline/draft 字段合并、rules 完整路径身份逐规则逐字段合并；
- 配置 Import/Reset 立即应用并同步运行态/磁盘/草稿；
- PC Remote 端口编辑不被周期刷新覆盖，多选/batch/editor 一致性，普通保存不误改隐藏 enabled；
- Remote start/apply 保证非空 token，空 token 请求拒绝；
- Release/Installer 版本来自 Cargo，Rust 工具链固定；
- 放映命令优先按活动文稿路径匹配窗口，不任意操作第一个窗口；
- 多窗口目标歧义保持“有放映”事实，具体命令安全失败。

除用户实测重新证明问题外，不重做上述修复。

## RC-2 已有执行记录与证据边界

Codex 报告 55 passed / 0 failed / 1 ignored；fmt、clippy、Release build、现有打包通过。

已有真实进程记录：F3/F4/F5、HTTP 鉴权与计时、本机 LAN、端口回退、配置 Apply/Cancel/Import/Reset、Portable 持久化、隔离安装/同版本覆盖/卸载，以及 `--show-settings` 模式启动退出。

这不是 ChatGPT 在用户电脑复跑的结果。原始日志仍在本机 `target/rc-temp/`；旧 CI 不作为当前 Rust 验收依据。

仍未可靠完成：正常托盘/Timer 入口和退出、PC Remote 原生交互、Office 实机（测试前有用户文稿而未操作）、手机跨设备、混合 DPI 拖动、实际听感和用户旧配置/旧安装升级。统一清单已按这些缺口组织，未测不得写成通过。

五张 rc-review PNG 来自离屏软件渲染及部分示例数据，不能替代真实 Windows 字体、焦点、跨屏或连接验证。ChatGPT 本次未能直接打开 PNG，不宣称图片观感已审通过。

## 当前本地产物与环境

RC-2 最后报告目录为：`E:\快传\计时器\v1.0\artifacts\release\v1.13.0\`。此前 J 盘路径属于前轮记录，不要直接套用。不同电脑以实际本轮目录为准，不要混用旧版同为 1.13.0 的包。

RC-2 报告项目 Rust 1.92.0 构建成功，MSVC/Windows SDK/打包工具可用；双屏为 150%/125%。PowerPoint/WPS 安装存在不代表实际控制通过。工具路径变化不允许成为升级 Slint/Cargo 依赖或改产品逻辑的理由。

## 后续工作边界

没有新的用户故障反馈就停止。无需为纯文档修改重跑构建、安装实验或打包。

用户按五组清单反馈通过/失败/未测/不适用。Codex 负责对明确故障定向定位、最小修复和相关回归；不顺手重构、不增加测试框架。明显不可用和数据/文稿风险阻断发布；普通视觉偏好集中记录，依据用户明确选择处理，不自行重做整套界面。

保持功能完整与用户批准的界面方向。不要将“终审”解释成自动发布授权：未经用户明确要求，不创建 Release/Tag、不合并默认分支、不修改版本或依赖。
