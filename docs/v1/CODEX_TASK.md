# 当前任务：设置窗口 / 跨 DPI 定向修复源码审核通过，等待真机复核

当前分支：`codex/v1-06-manual-test`

## 最新审核结论

2026-09-09 的设置窗口显示问题定向修复已完成源码审核，可以保留当前实现。

本轮属于对已记录真机反馈的定向修复，不是预防性 UX-05，也不要继续扩大为 Timer / 大屏 / 时间到窗口的泛化 DPI 重构。

确认可保留：

- 删除设置模型创建阶段固定 `PhysicalSize(900, 650)`，避免把期望的逻辑尺寸误当成物理像素；
- 首次显示时依据当前 HWND 所在屏幕 DPI，把 900×650 逻辑尺寸换算成实际物理尺寸；
- 已存在设置窗口重新打开时继续保留当前客户区尺寸，不增加窗口尺寸持久化配置；
- `FieldRow` 标签继续自动换行，并使用整行扣除上下留白后的绘制区域；
- `WM_DPICHANGED` 不再用消息期间的 `GetDpiForWindow` 误判旧 DPI；
- 复用已有 Windows subclass reference data 记录上一次 DPI，不新增独立 DPI 状态机；
- DPI 消息继续转发给 Winit / Slint，使框架自身收到 scale-factor 变化；
- 非最大化状态下继续做现有客户区尺寸校正，最大化时不额外强改客户区；
- 设置窗口与 PC Remote 共用现有 DPI 处理器，因此真实跨屏复核同时覆盖两者；
- 未修改设置业务语义、Remote 协议、Office 控制、Timer 核心、配置格式或文件规则逻辑。

源码层面没有发现新的重试链、备用路径、尺寸恢复链、复杂状态机或完整性校验。当前 DPI reference data 属于既有 subclass 的最小状态，不按“过度防御”处理。

## 验证审核

Codex 记录本轮修复过程中为诊断运行了 debug build；进入新的本地手测包阶段后统一运行了一次：

```powershell
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo build --release
```

记录结果均通过，`cargo test` 为 37 passed、0 failed、1 ignored（既有 Office 真机测试）。

这次完整四项验证发生在明确的本地手测包打包阶段，符合风险分层策略；**不要把它恢复成后续每轮修改都必须执行的固定门禁。**

当前待测包：`E:/快传/计时器/tests/FlyPPTTimer-v1.13.0-SettingsFix01-20260909.zip`（同名目录内可直接运行）。这是 review 手测包，不是正式 Release。

---

## 现在停止继续编码，等待以下真机复核

请不要继续猜测或扩大 DPI 修复。下一步只等待用户对当前包给出真实结果。

最小手测范围：

1. 设置窗口首次打开时尺寸是否恢复到合理的约 900×650 逻辑大小，而不是在 150% 缩放下明显偏小；
2. 关闭设置再重新打开，尺寸是否仍合理且不会再次缩小或放大；
3. 六个设置页的中英文标签是否完整，尤其长标签是否仍有缺字/截断；
4. 在 150% 屏与 125% 屏之间拖动设置窗口，缩放是否稳定，文字和客户区是否出现跳动、尺寸翻倍/缩半或裁切；
5. 设置窗口最大化 → 还原是否正常，不出现错误客户区尺寸；
6. PC Remote 在相同跨 DPI 拖动场景下是否稳定，因为它共用当前 DPI 处理器；
7. UX-04 的 Tab / Shift+Tab、Enter / Space、退出确认层和批量设置层交互是否仍正常。

如果用户报告新问题，只记录**能够稳定复现该问题的最小条件**，不要要求完整设备矩阵。

---

## UX-05 边界保持不变

当前设置窗口 / PC Remote 的 DPI 定向修复不等于已经进入 UX-05。

Timer / 大屏 / 时间到窗口只有在用户提供对应真实复现后才允许下发代码修改任务。不要因为这次 DPI 修复顺便修改：

- Timer 默认尺寸、字号、圆角、位置或配色；
- 六套 Timer 配色与用户自定义颜色；
- 大屏目标与显示策略；
- 时间到窗口；
- Timer 跨屏创建 / 移动时序；
- 计时核心行为。

如果 Timer / 大屏 / 时间到没有真实异常，可以让 UX-05 最终以“未发现需要修改的问题”结束，而不是强行制造改动。

---

## 持续编码约束

- 不增加 SHA / Hash / checksum / 完整性验证；
- 不重复验证同一事实；
- 不增加无必要重试、双重读取、备用路径、备份/恢复链或额外状态机；
- 不建立 DPI / 截图 / 设备测试矩阵；
- 不为未复现的问题预先增加 fallback；
- 保持最少必要逻辑。

## 持续测试策略

- 小改动 / 定向 Bug：只跑直接相关的最小测试；
- Timer 核心 / 配置 / Remote 协议变化：跑对应模块或行为定向测试；
- 一次任务完成准备给 ChatGPT / 用户手测：最多一次 `cargo test`，已有足够定向验证时可跳过；
- 只有明确进入 merge / package / release 收口阶段时，统一运行一次 fmt / clippy / test / release build。

## 当前动作

- 设置窗口 / 共享 DPI 定向修复：源码审核通过，等待用户真机复核；
- UX-01～UX-04：已通过源码审核，真实视觉/键盘/窗口交互仍以用户手测为准；
- UX-05：没有 Timer / 大屏 / 时间到真实复现时继续保持停止；
- Codex 当前不要继续修改代码；
- 不创建 Release / Tag，不进入正式发布流程。
