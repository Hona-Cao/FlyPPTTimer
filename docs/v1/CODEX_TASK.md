# 当前状态：RC-2 终审完成，源码冻结，等待统一手测反馈

日期：2026-09-11
分支：`codex/v1-06-manual-test`
审阅成果提交：`633a830d78ca02be05994364d86f93eaf40a263e`
候选源码基点：`952226d4c114b59334b13d0a7b44c60d89b9f030`
程序版本：`1.13.0`

## 终审决定

接受当前实现为代码冻结候选版。RC-2 仅新增验收文档，没有修改程序源码；ChatGPT 已审核提交差异和逐项验证记录。本次不再下发主动扫描、重构或新功能任务。

这不是无条件正式发布批准。PowerPoint/WPS 真机、桌面/PC Remote 原生入口、手机跨设备、混合 DPI、实际声音、适用的旧版升级仍未全部验收。不能把未测项目改写为通过。

## 唯一剩余人工验收入口

阅读：`docs/v1/RC_MANUAL_TEST.md`。

该文件已统一终审结论、RC-2 产物位置、已验证且无需重复的内容、剩余五组实际操作、通过标准和反馈模板。此前各轮分散的“待手测”不再分别派发。

产品目标不变：全部既有功能稳定、界面美观协调且操作舒适。保留当前用户已满意的布局；遵守 `APPROVED_PRODUCT_DEVIATIONS.md`，不得恢复随机端口控件或 PC Remote 的放映控制按钮。

## Codex 现在应做什么

1. 拉取最新文档，按 AGENTS 必读顺序确认状态。
2. 没有新的用户故障反馈时停止编码；不重新运行已经通过的完整构建/安装实验，不重新打包同一份源码。
3. 用户需要定位产物时，仅核对 `RC_CANDIDATE.md` 中本轮实际目录；不要拿旧 UX13/RC-1 同版本包替代，不覆盖用户真实配置。
4. 收到手测反馈后，按清单编号记录复现步骤，区分通过、真实失败、工具/环境无法验证和不适用。
5. 只有明确反馈形成的待修问题才进入定向定位；P0/P1 做最小修复与相关回归，不顺手重构。普通视觉偏好集中汇总，按用户明确选择处理，不擅自启动整套 UI 重做。
6. 未经用户明确授权，不创建 Release/Tag、不合并到默认分支、不改变版本或依赖。

## 证据边界

RC-2 报告为 55 passed、0 failed、1 ignored，fmt/clippy/Release build/现有打包通过；实际 Windows 结果及限制见 CODEX_RESULT。ChatGPT 审阅不等于亲自在用户电脑复跑。原始日志仍在本机 target/rc-temp/。

仓库五张 rc-review 图片源自 headless capture，不能替代原生窗口、跨屏、焦点和连接状态验证，也不能据此宣称界面最终验收通过。

当前应结束主动 Debug 循环，等待用户的一次性体验反馈，而不是自行制造下一轮任务。
