# 1.13.0 用户反馈整改包：剩余体验确认

更新：2026-09-11。分支：`codex/v1-06-manual-test`。只测试本轮最终 `FlyPPTTimer-v1.13.0-feedback-win-x64.zip`，源码 SHA 见包内 BUILD.txt；不要混用旧 RC 的同版本 EXE。

解压到新文件夹，保留 EXE 旁的两个 DLL，双击运行即可。包内配置为空规则、空 token，首次启动自动生成鉴权 token。不覆盖原文件夹或原配置，不需要开发工具。完整程序侧验证见 CODEX_RESULT.md；下面只保留设备和体验边界。

1. **跨屏观感**：保持原 150%/125% 双屏，用自己先前沿两屏边缘绕行的路径各拖一次 Settings、PC Remote，确认大小与控件观感正常。实现侧已完成各 3 圈、10 次往返、拉伸、最大化还原和同进程重开，无累计漂移。
2. **手机热点扫码**：电脑连手机 Wi-Fi 热点后，用 Remote 页唯一主要二维码打开页面，试 Start/Pause/Resume/Reset；用临时文稿试翻页，再重启服务重新扫码。电脑侧 LAN HTTP/鉴权已通过，尚无真实手机验收。若失败，只记录页面表现（超时、拒绝连接、403/404 或页面正常但命令失败）；不要反复猜 IP，也不必先改安全设置。反馈截图遮住二维码/token。
3. **真实听感与安全软件**：用自己的提示音听首次及第二次提醒，再听 TTS，确认音量和静音恢复。原生测试已覆盖 WAV/MP3/WMA/M4A、中文空格路径、直接 WMP 回退及 SAPI；仍缺主观听感。如有拦截，记录安全软件名称和被拦截进程/入口，遮住私人路径；不需要关闭杀软。安装更新仍需明确确认。
4. **单屏/投影放映效果**：用可丢弃文稿短计时，到时两屏应全黑、中心显示“时间到”；普通点击、下一页和正常结束放映不应解除，主持人 F4 或 Remote 重置应解除，退出应清理。双屏三轮几何/四角纯黑、点击、F4、Remote 重置及退出已测；独立单屏、现场投影以及实际放映结束/下一页联动仍待确认（额外 COM 启动补测返回 80080005）。另试“仅提示/退出放映”时不出现黑色遮罩。

反馈只需写项目编号、现象和是否通过；无需重做历史清单、编译、COM 故障注入或 DPI 日志分析。本包用于 review 验收，未发布 Release/Tag，未合并默认分支。
